import { MarkAttributeDirective } from './mark-attribute.directive';

describe('MarkAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
