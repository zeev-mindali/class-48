import { MyWifeDirective } from './my-wife.directive';

describe('MyWifeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyWifeDirective();
    expect(directive).toBeTruthy();
  });
});
