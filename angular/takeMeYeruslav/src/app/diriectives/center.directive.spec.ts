import { CenterDirective } from './center.directive';

describe('CenterDirective', () => {
  it('should create an instance', () => {
    const directive = new CenterDirective();
    expect(directive).toBeTruthy();
  });
});
