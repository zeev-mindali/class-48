import { OutOfBoundsDirective } from './out-of-bounds.directive';

describe('OutOfBoundsDirective', () => {
  it('should create an instance', () => {
    const directive = new OutOfBoundsDirective();
    expect(directive).toBeTruthy();
  });
});
