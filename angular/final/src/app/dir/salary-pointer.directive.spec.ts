import { SalaryPointerDirective } from './salary-pointer.directive';

describe('SalaryPointerDirective', () => {
  it('should create an instance', () => {
    const directive = new SalaryPointerDirective();
    expect(directive).toBeTruthy();
  });
});
