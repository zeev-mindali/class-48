import { MarkerDirective } from './marker.directive';

describe('MarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkerDirective();
    expect(directive).toBeTruthy();
  });
});
